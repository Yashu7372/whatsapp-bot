package com.whatsappbot.video.engine;

/**
 * High-level output mode. Provider adapters decide whether they support a mode.
 */
public enum GenerationMode {
    FACELESS,
    PRESENTER,
    DIALOGUE
}
