<section class="scene scene--hero span-{{scene.display.span}}">
  <div class="scene__eyebrow">{{scene.data.article.series_name}}</div>
  <h1 class="hero__title">{{scene.data.article.title}}</h1>
  <div class="hero__body markdown-body">{{{scene.body_html}}}</div>
</section>
